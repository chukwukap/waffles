import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Waffles",
  description:
    "Waffles is a tournament-based game where users try to recognize patterns in images ranging from memes, celebrity images, football, basketball, any sport, or any movie, basically any interesting thing. That's the thing about Waffles. It is designed as a live, cinematic experience where speed, memory, and intuition collide in a test of who can recognize the most patterns, the fastest.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
